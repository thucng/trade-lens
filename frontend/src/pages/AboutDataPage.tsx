export function AboutDataPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">About Data</h1>
      <p className="mt-2 text-slate-600">
        TradeLens uses the latest available official trade data from configured
        public trade data sources. Official trade data may lag behind the
        current calendar year.
      </p>
    </section>
  )
}
